# 项目优化建议

当前项目更像一个“可演示的 LangGraph 旅游规划 Demo”，离“稳定可维护的产品化服务”主要差在状态结构、工具稳定性、错误处理、测试、配置安全和部署规范。前几阶段已完成工具稳定、基础测试和阶段化 LangGraph 重构，历史 `new/` 参考目录已经迁移并删除；后续重点应转向配置、缓存、持久化和产品化。

## 优化清单

| 维度 | 主要问题 | 一句话优化方向 |
|---|---|---|
| 架构分层 | 优化前主链路只有 `agent` 和 `tools` 两个节点，完整旅游流程主要靠大 Prompt 驱动；第三阶段后已拆为意图识别、目的地澄清、景点采集、行程规划、交通校验、餐住推荐、最终回复等多节点 LangGraph。 | 后续重点不是再拆节点，而是增强节点质量、节点级事件输出、结构化日志和真实对话验收。 |
| 状态设计 | 已升级为 `TravelPlannerState`，包含目的地、景点、行程、交通、餐住和校验问题。 | 后续补充持久化和更细粒度状态校验。 |
| 双版本并存 | `new/` 参考实现已迁移并删除，正式入口集中在根目录实现。 | 后续避免再引入平行实现，实验代码进入文档或分支。 |
| Prompt 维护 | `prompts/main.py` 是超长万能 Prompt，规则、流程和输出格式耦合。 | 拆成阶段 Prompt，并让每个节点只负责一个明确业务目标。 |
| 工具返回格式 | 已统一为 `success / data / error` 结构，并集中到正式 `tools/base.py`。 | 后续补充更多边界测试和真实联调。 |
| 外部请求稳定性 | `tools/nearby.py` 和 `tools/transportation.py` 没有请求超时和 HTTP 状态检查。 | 所有外部 API 调用加 `timeout`、`raise_for_status`、重试、错误码解析和降级返回。 |
| Selenium 抓取 | `tools/attractions.py` 每次工具调用都启动 Chrome，慢且容易失败。 | 优先接入稳定数据源/API，必要时复用浏览器池并增加缓存和失败降级。 |
| 异步阻塞 | FastAPI 流式接口是 async，但工具内部大量同步 `requests`、Selenium 和 `time.sleep`。 | 将阻塞工具放到线程池，或改用异步 HTTP 客户端，避免一个请求卡住事件循环。 |
| 缓存策略 | 经纬度、景点、POI、路线等重复查询没有统一缓存，只有坐标工具里一个进程内计数器。 | 为外部查询增加 TTL 缓存，并按目的地、坐标、参数生成稳定 cache key。 |
| 会话持久化 | LangGraph 使用 `InMemorySaver`，服务重启后会话全部丢失。 | 替换为 SQLite/Postgres/Redis checkpointer，保存会话状态和关键中间结果。 |
| API 安全 | `api/main.py` CORS 全放开且允许 credentials。 | 本地开发和生产配置分离，生产只允许可信前端域名。 |
| 输入校验 | `thread_id`、`message`、工具参数缺少长度、格式和范围限制。 | 用 Pydantic 增加字段约束，限制空消息、超长输入、非法坐标和过大分页。 |
| 日志与观测 | 工具里有多处 `print`，错误日志不统一，缺少请求 ID 和工具耗时。 | 全部改为结构化 logging，记录 `thread_id`、`tool_name`、`latency`、`status` 和 `error_code`。 |
| 前端体验 | `templates/index.html` 能跑，但缺少取消生成、新建会话、历史会话、错误重试，也缺少长任务开始后的用户可见进度反馈和降级提示框。 | 增加停止生成、重新发送、新会话、工具调用折叠、加载状态、阶段进度消息、降级提示框和错误恢复。 |
| 流式协议 | 当前返回 NDJSON，可用但与常见前端生态和代理兼容性一般；advanced 分支只透出最终回复节点，用户等待长任务时缺少“已识别目的地、正在获取景点、正在生成行程”等中间反馈，也无法单独表达 fallback、缓存旧数据、跳过工具等降级操作。 | 可以保留 NDJSON，也可增加 SSE 版本，明确 `progress`、`degraded`、`chunk`、`tool_start`、`tool_end`、`error`、`done` 等事件类型和前端解析规范。 |
| 模型配置 | 默认模型、base_url、temperature 写在代码里，模型名还和 README 描述有些混乱。 | 建立 `settings.py`，统一从环境变量读取模型、API 地址、超时、重试和缓存开关。 |
| 依赖管理 | 只有 `requirements.txt`，没有开发依赖、锁文件、格式化工具配置。 | 增加 `pyproject.toml`，配置 ruff/black/pytest，并区分 runtime 与 dev 依赖。 |
| 测试缺失 | 项目里没有测试文件或 pytest 配置。 | 先写工具层单元测试和 API 冒烟测试，再补 LangGraph 节点级集成测试。 |
| 安全与密钥 | `.env` 被忽略是对的，但缺少 `.env.example` 和启动前配置校验。 | 提供 `.env.example`，服务启动时集中校验必需环境变量并给出清晰错误。 |
| 文档准确性 | README 里仍有旧路径、Gradio/FastAPI 描述混杂。 | 更新 README，明确两个入口、推荐启动方式、环境变量和常见故障。 |
| 代码整洁 | 代码中教学型长注释较多，部分命名有拼写问题如 `infomation_to_save`。 | 保留关键业务注释，清理冗长解释，统一命名、类型标注和代码风格。 |
| 文件职责 | `tools/static_map.py` 里函数名和 `locations.py` 冲突，且直接写当前目录图片。 | 更名为 `get_static_map`，输出到受控目录，并返回可访问路径或二进制响应。 |
| 生产部署 | 当前只有本地 `uvicorn` 启动脚本，没有容器、健康检查细节和配置模板。 | 增加 Dockerfile、生产启动命令、日志目录、健康检查和部署说明。 |

## 建议优先级

### 第一阶段：先做稳定性

优先统一工具返回、补超时和错误处理、去掉 `print`、加基础测试。

这一阶段收益最大，因为当前 Agent 的稳定性很大程度取决于工具层能不能给出可恢复、可解释、可测试的结果。

### 第二阶段：做结构化重构

结构化状态和多节点图已迁移到正式入口，下一步重点是节点质量、事件输出和真实对话验收。

这一阶段的目标不是增加功能，而是降低大 Prompt 控制复杂业务流程带来的不确定性。

### 第三阶段：做产品化

持久化会话、缓存外部查询、完善前端交互、收紧 CORS 和配置管理。

这一阶段面向真实运行环境，重点解决服务重启丢状态、外部接口慢、前端交互弱和配置不安全等问题。

## 最值得先做的一项

如果只选一个最值得先做的优化，建议先统一工具层返回和异常处理。

原因是它直接决定：

- Agent 是否能在工具失败后继续规划或向用户解释；
- 前端是否能展示清晰的工具调用失败原因；
- 测试是否能稳定覆盖工具成功、失败、空结果、超时等场景；
- 后续阶段化 LangGraph 节点是否能基于统一数据结构编排业务流程。

推荐统一返回格式：

```python
{
    "success": True,
    "data": {},
    "error": None,
}
```

失败时：

```python
{
    "success": False,
    "data": None,
    "error": {
        "message": "具体错误类型",
        "detail": "具体错误详情",
    },
}
```

## 可落地的后续路线

1. 将 `tools/nearby.py`、`tools/transportation.py`、`tools/locations.py`、`tools/web_search.py`、`tools/attractions.py` 的返回结果统一包装。
2. 给所有外部请求增加 `timeout`、`raise_for_status` 和明确异常捕获。
3. 删除工具里的 `print`，改成 `logging`。
4. 新增 `tests/`，先覆盖工具层，不直接依赖真实外部 API，可用 mock 模拟响应。
5. 继续完善正式 `TravelPlannerState` 的持久化、校验和节点级测试。
6. 将 `prompts/main.py` 拆成意图识别、澄清、规划、校验、最终回复等阶段 Prompt。
7. 将 `run_fastapi.py`、`api/main.py`、模型工厂和工具超时统一接入 `settings.py`。
8. 更新 README，明确当前推荐入口、环境变量、启动命令和已知限制。


