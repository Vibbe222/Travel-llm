# 项目总体优化建议

更新时间：2026-06-30

当前项目已经不再只是“可演示的 LangGraph 旅游规划 Demo”。从代码结构看，工具稳定性、结构化状态、多节点 LangGraph、Prompt 拆分、统一配置、Redis 缓存/checkpoint、流式前端体验和基础测试都已经落地。后续重点应从“搭工程骨架”转向“收尾一致性、测试绿灯、输入校验、文档同步和部署规范”。

## 达成度总览

| 维度 | 原主要问题 | 当前状态 | 后续建议 |
|---|---|---|---|
| 架构分层 | 主链路只有 `agent` 和 `tools` 两个节点，复杂流程靠大 Prompt 驱动。 | 已完成。当前有意图识别、澄清、景点采集、行程规划、交通校验、餐住推荐和最终回复等多节点。 | 继续做节点级质量评估和真实对话验收，不建议再盲目拆节点。 |
| 状态设计 | 旧状态只维护消息，缺少业务中间结果。 | 已完成。`TravelPlannerState` 已包含目的地、约束、景点、行程、交通、餐住、校验问题等字段。 | 补更严格的状态校验和多轮修改策略。 |
| 双版本并存 | `new/` 参考实现和正式入口并存。 | 已完成。`new/` 目录已删除，正式入口集中在根目录模块。 | 实验实现进入分支或文档，不再引入平行目录。 |
| Prompt 维护 | `prompts/main.py` 是超长万能 Prompt。 | 已完成。Prompt 已拆为 `intent`、`clarify`、`planner`、`validation`、`finalize`。 | 补节点级评测样例，持续验证 JSON 输出稳定性。 |
| 工具返回格式 | 工具返回结构不统一，失败难恢复。 | 已完成。已统一为 `success/data/error`，集中在 `tools/base.py`。 | 保持新增工具使用同一结构。 |
| 外部请求稳定性 | 缺少超时、HTTP 状态检查和异常处理。 | 已完成。高德、搜索等外部请求已接入超时、重试、`raise_for_status()` 和错误包装。 | 针对临时失败可继续调优重试策略和熔断策略。 |
| Selenium 抓取 | 每次抓取启动浏览器，慢且易受页面变化影响。 | 部分完成。失败时会降级到网页搜索，不再中断主流程。 | 长期建议替换为稳定 API 或可缓存的数据源。 |
| 异步阻塞 | FastAPI async 接口内有同步请求和 Selenium。 | 部分完成。流程可运行并有降级，但同步工具仍可能占用请求处理时间。 | 后续可将阻塞工具放入线程池，或逐步迁移到异步 HTTP 客户端。 |
| 缓存策略 | 经纬度、景点、POI、路线、搜索没有统一缓存。 | 已完成。已接入 Redis TTL 缓存，并按参数生成稳定 cache key。 | 前端可继续展示缓存命中、缓存降级等状态。 |
| 会话持久化 | 只用 `InMemorySaver`，服务重启丢状态。 | 已完成。已实现 Redis-backed checkpointer，Redis 不可用时回退内存。 | 生产建议评估官方 Redis/Postgres checkpointer。 |
| API 安全 | CORS 全放开且允许 credentials。 | 已完成。CORS 已从 `settings.py` 读取，生产环境禁止通配来源。 | 补请求字段长度、格式和范围约束。 |
| 输入校验 | `thread_id`、`message`、工具参数缺少约束。 | 部分完成。已有 Pydantic 请求体，但字段仍是普通字符串。 | 增加 `min_length`、`max_length`、模式校验和坐标范围校验。 |
| 日志与观测 | 工具里有 `print`，日志不统一。 | 大部分完成。工具主链路已使用 logging，记录工具名、耗时、状态和错误。 | 清理仅用于本地示例的 `__main__` 调试 `print()`。 |
| 前端体验 | 缺少取消生成、新会话、历史、错误重试、进度和降级提示。 | 已完成。单文件 Vue 前端已有停止生成、新建会话、历史会话、导出、工具折叠、进度、错误和降级提示。 | 产品化时迁移到独立前端工程并补端到端测试。 |
| 流式协议 | NDJSON 可用但缺少清晰事件类型。 | 已完成。已规范化 `progress/chunk/tool_start/tool_end/tool_error/degradation/error/done`。 | 如需兼容更多代理或前端生态，可增补 SSE 版本。 |
| 模型配置 | 模型名、base_url、temperature 分散在代码里。 | 已完成。`settings.py` 统一读取模型、DashScope、超时、重试、缓存、CORS 和搜索配置。 | 修复当前配置测试与实际默认值不一致的问题。 |
| 依赖管理 | 只有 `requirements.txt`，缺少开发依赖和格式化配置。 | 部分完成。`requirements.txt` 已包含运行和测试依赖。 | 后续可增加 `pyproject.toml`，配置 ruff/black/pytest。 |
| 测试缺失 | 没有测试文件或 pytest 配置。 | 部分完成。已有 79 个测试，覆盖工具、API、图流程、配置、Redis 和流式事件。 | 当前全量 pytest 为 `76 passed / 3 failed`，需优先修复。 |
| 安全与密钥 | 缺少 `.env.example` 和集中校验。 | 已完成。已有 `.env.example`，配置集中校验。 | 确保文档和日志永远不暴露真实 key。 |
| 文档准确性 | README 和其他文档有旧路径、旧流程描述。 | 部分完成。README 和总览文档已有更新，但 `docs/interview-prep/` 中仍有旧 `InMemorySaver`、旧路径等描述。 | 下一轮重点同步旧面试文档和运行流程文档。 |
| 代码整洁 | 教学型长注释较多，命名和本地产物需要清理。 | 部分完成。核心结构清晰，但仍有 `.idea`、`__pycache__`、`.pytest_cache` 等本地产物。 | 清理本地缓存产物，并逐步精简过长解释型注释。 |
| 文件职责 | `static_map.py` 命名和输出路径不够规范。 | 未重点处理。当前优化主线尚未覆盖静态地图模块。 | 后续可更名为 `get_static_map`，输出到受控目录或返回可访问资源。 |
| 生产部署 | 缺少容器、健康检查和生产启动模板。 | 待完成。已有 `/health`，但缺少 Dockerfile 和部署说明。 | 增加 Dockerfile、生产启动命令、日志目录、健康检查和部署文档。 |

## 当前测试状态

最近一次执行：

```powershell
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
79 collected
76 passed
3 failed
```

失败集中在配置和 Tavily 搜索测试：

- 默认 `WEB_SEARCH_PROVIDER` 预期与当前配置不一致；
- 生产环境缺少 API key 的校验预期与当前实现不一致；
- Tavily 缺少 key 时应直接返回 `missing_tavily_api_key`，当前实际进入请求并返回超时。

因此，“测试体系建设”已达成，但“全量测试稳定通过”尚未达成。

## 当前最值得先做的事项

最优先不是继续加新功能，而是修复配置和测试一致性。

原因：

- 当前失败测试会降低后续改动信心；
- 配置默认值会影响搜索 provider、API key 校验和本地/生产行为；
- Tavily key 缺失如果不能快速失败，会导致无意义的外部请求和较慢错误反馈；
- 这些问题范围集中，修复后能立即恢复全量回归测试价值。

建议先处理：

1. 明确默认搜索 provider 是 `duckduckgo` 还是 `tavily`。
2. 统一 `.env.example`、`settings.py`、`tests/test_settings.py` 的默认值。
3. 明确生产环境是否默认强制要求 `DASHSCOPE_API_KEY` 和 `AMAP_API_KEY`。
4. 修复 Tavily 缺少 API key 时的快速失败逻辑或更新测试预期。
5. 重新执行全量 pytest，确认回到 100% 通过。

## 后续路线

### 第一优先级：收尾一致性

- 修复 3 个失败测试。
- 同步 Tavily / DuckDuckGo 配置口径。
- 清理旧文档中的过期描述。
- 清理本地缓存产物和仅示例用途的调试 `print()`。

### 第二优先级：接口质量

- 给 `ChatRequest` 增加 Pydantic 字段约束。
- 对工具参数增加范围校验，例如坐标、半径、分页大小。
- 统一错误码和用户可读错误文案。

### 第三优先级：生产化

- 增加 Dockerfile 和部署说明。
- 补生产日志、健康检查和配置模板。
- 评估官方 Redis/Postgres checkpointer。
- 如前端继续扩展，迁移到独立前端工程并补端到端测试。

## 已完成能力清单

- 多节点 LangGraph 工作流。
- 结构化 `TravelPlannerState`。
- 分阶段 Prompt。
- JSON 输出 fallback。
- 工具统一返回结构。
- 外部请求超时、重试和错误包装。
- Selenium 失败降级。
- Redis TTL 缓存。
- Redis-backed checkpoint。
- 统一 `settings.py` 配置。
- `.env.example`。
- FastAPI NDJSON 流式接口。
- 前端进度、工具面板、降级提示和历史会话。
- 工具、API、图流程、配置、Redis、流式事件测试。

## 未完成或仍需增强

- 全量测试当前未完全通过。
- 输入字段约束仍偏弱。
- `docs/interview-prep/` 中仍有旧流程描述。
- 生产部署文档和 Dockerfile 尚未补齐。
- 官方级 checkpoint 存储方案尚未评估或替换。
- 静态地图模块仍未纳入本轮工程化重构。
