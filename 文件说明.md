# 项目文件说明

本文档说明项目中各目录和主要文件的作用，便于快速了解代码结构、运行入口和文档资料位置。

## 根目录文件

| 文件 | 作用 |
|---|---|
| `.env` | 本地环境变量文件，保存模型、地图、搜索、Redis 等真实配置。包含敏感信息，不应提交到 Git。 |
| `.env.example` | 环境变量示例文件，用于说明项目运行所需的配置项。 |
| `.gitignore` | Git 忽略规则，控制缓存、虚拟环境、IDE 配置等本地文件不进入版本库。 |
| `README.md` | 项目主说明文档，包含功能概览、项目结构、安装、启动、接口、工作流和测试说明。 |
| `requirements.txt` | Python 依赖清单，包括 FastAPI、LangGraph、LangChain、Gradio、Selenium、Redis、pytest 等依赖。 |
| `run_fastapi.py` | FastAPI 服务启动入口，运行后启动后端接口和 `templates/index.html` 单页前端。 |
| `webrun.py` | Gradio 演示入口，保留原有网页聊天演示方式。 |
| `chat_service.py` | 聊天服务层，负责创建会话、构造 LangGraph 配置、归一化流式事件、输出前置计划摘要、处理工具错误和降级提示。 |
| `settings.py` | 统一配置中心，负责读取和校验环境变量，提供模型、接口鉴权、CORS、第三方 API、Redis、重试和超时等配置。 |
| `基于LangGraph的旅游规划机器人.md` | 项目介绍类文档，说明项目背景、核心能力、目录结构和快速启动方式。 |
| `项目总体优化建议.md` | 项目优化建议文档，记录整体优化清单和阶段优先级。 |
| `项目优化计划.md` | 分阶段优化计划，列出各阶段任务、产出和验收重点。 |
| `修改内容说明.md` | 已完成修改的汇总说明，记录架构、状态、工具、配置、前端和测试等变化。 |

## 业务源码目录

### `api/`

| 文件 | 作用 |
|---|---|
| `api/__init__.py` | 标记 `api` 为 Python 包。 |
| `api/main.py` | FastAPI 应用定义，提供首页、健康检查、创建会话和聊天流式接口。 |

### `agents/`

| 文件 | 作用 |
|---|---|
| `agents/__init__.py` | 标记 `agents` 为 Python 包。 |
| `agents/agents.py` | LLM Agent 封装，提供同步和异步调用模型的统一接口。 |
| `agents/stage_executor.py` | 阶段执行器，负责按 Prompt 调用模型、解析 JSON 输出，并在失败时使用 fallback 结果。 |

### `graph/`

| 文件 | 作用 |
|---|---|
| `graph/__init__.py` | 标记 `graph` 为 Python 包。 |
| `graph/graph.py` | LangGraph 工作流创建入口，组装节点、路由和 checkpointer。 |
| `graph/nodes.py` | 工作流节点实现，包括意图识别、目的地澄清、景点采集、行程规划、交通校验、周边补充和最终回复。 |
| `graph/routers.py` | LangGraph 条件路由函数，根据状态决定下一步节点。 |
| `graph/checkpoint.py` | Checkpoint 构建逻辑，优先使用 Redis 持久化会话状态，失败时回退到内存。 |

### `models/`

| 文件 | 作用 |
|---|---|
| `models/__init__.py` | 标记 `models` 为 Python 包。 |
| `models/factory.py` | 模型工厂入口，根据模型名称创建对应 LLM 实例。 |
| `models/qwen_factory.py` | 阿里云百炼 OpenAI 兼容接口的模型创建逻辑。 |

### `prompts/`

| 文件 | 作用 |
|---|---|
| `prompts/__init__.py` | 统一导出各阶段 Prompt。 |
| `prompts/main.py` | Prompt 注册表，集中管理意图识别、澄清、规划、校验和最终回复 Prompt。 |
| `prompts/intent.py` | 意图识别 Prompt，用于判断用户是否已给出明确目的地和旅行需求。 |
| `prompts/clarify.py` | 澄清 Prompt，用于在信息不足时生成追问。 |
| `prompts/planner.py` | 行程规划 Prompt，用于生成结构化每日行程。 |
| `prompts/validation.py` | 交通校验 Prompt，用于判断行程中的交通安排是否合理。 |
| `prompts/finalize.py` | 最终回复 Prompt，用于把结构化结果整理为用户可读的旅行方案。 |

### `states/`

| 文件 | 作用 |
|---|---|
| `states/__init__.py` | 标记 `states` 为 Python 包。 |
| `states/state.py` | LangGraph 状态类型定义，描述目的地、约束、景点、行程、交通、周边 POI、错误和最终结果等字段。 |

### `tools/`

| 文件 | 作用 |
|---|---|
| `tools/__init__.py` | 标记 `tools` 为 Python 包。 |
| `tools/base.py` | 工具公共基础模块，提供统一成功/失败返回结构、日志配置、参数摘要、HTTP 重试和错误处理。 |
| `tools/attractions.py` | 景点采集工具，使用 Selenium 抓取马蜂窝景点信息，失败时可降级到 Web 搜索。 |
| `tools/web_search.py` | Web 搜索工具，支持 Tavily 和 DuckDuckGo 两种 provider。 |
| `tools/locations.py` | 高德地理编码工具，根据地点名称查询经纬度。 |
| `tools/nearby.py` | 高德周边 POI 搜索工具，用于查询餐饮、酒店等附近推荐。 |
| `tools/transportation.py` | 高德公共交通路线规划工具，用于校验景点之间的交通可行性。 |
| `tools/save.py` | 保存工具返回的重要信息，并清理历史中不再需要的工具上下文。 |
| `tools/static_map.py` | 静态地图相关工具，根据地址查询坐标并辅助生成地图展示。 |

### `utils/`

| 文件 | 作用 |
|---|---|
| `utils/__init__.py` | 标记 `utils` 为 Python 包。 |
| `utils/helper.py` | 通用辅助函数，包括当前时间、线程 ID 和聊天历史保存。 |
| `utils/redis_cache.py` | Redis 缓存服务，提供缓存 key 生成、JSON 读写和 Redis 不可用时的降级处理。 |

### `templates/`

| 文件 | 作用 |
|---|---|
| `templates/index.html` | FastAPI 返回的前端页面，提供聊天界面、流式输出、会话操作、调试面板、错误展示和导出能力。 |

## 测试目录

| 文件 | 作用 |
|---|---|
| `tests/conftest.py` | pytest 公共配置和 fixture，例如清理 settings 缓存。 |
| `tests/test_api_smoke.py` | FastAPI 接口冒烟测试，覆盖健康检查、CORS、创建会话和聊天流式请求结构。 |
| `tests/test_graph_checkpoint.py` | LangGraph checkpoint 测试，覆盖 Redis 不可用回退和快照恢复。 |
| `tests/test_graph_workflow.py` | 工作流节点和路由测试，覆盖结构化状态、澄清、景点采集、交通校验、周边补充和 fallback 行程。 |
| `tests/test_redis_cache.py` | Redis 缓存服务测试，覆盖缓存 key 稳定性、Redis 缺失降级和 JSON 读写。 |
| `tests/test_settings.py` | 配置读取和校验测试，覆盖开发/生产环境、CORS、API key 和搜索 provider。 |
| `tests/test_streaming_and_prompts.py` | 流式事件和 Prompt 测试，覆盖事件过滤、进度提示、降级事件、工具错误和前置摘要。 |
| `tests/test_tools_attractions.py` | 景点工具测试，覆盖 Selenium 失败 fallback、马蜂窝成功抓取、缓存策略和错误返回。 |
| `tests/test_tools_base.py` | 工具公共模块测试，覆盖返回协议、HTTP 重试、超时、不可重试状态码和 JSON 解析失败。 |
| `tests/test_tools_locations.py` | 地理编码工具测试，覆盖缺少 key、缓存命中、成功响应、业务错误、空结果和网络超时。 |
| `tests/test_tools_nearby.py` | 周边 POI 工具测试，覆盖参数校验、成功响应、空结果、缺少 key、业务错误和网络超时。 |
| `tests/test_tools_transportation.py` | 交通路线工具测试，覆盖成功响应、业务错误、缺少 key、无效参数和网络超时。 |
| `tests/test_tools_web_search.py` | Web 搜索工具测试，覆盖 DuckDuckGo 和 Tavily 的成功、空结果、限流、超时和缺少 API key。 |

## 文档目录

### `docs/interview-prep/`

| 文件 | 作用 |
|---|---|
| `docs/interview-prep/README.md` | 面试准备材料索引。 |
| `docs/interview-prep/project-walkthrough.md` | 项目讲稿，帮助按主链路和模块讲清项目实现。 |
| `docs/interview-prep/7-day-plan.md` | 7 天面试训练清单。 |
| `docs/interview-prep/问答.md` | 高频追问题库，整理项目介绍、架构、工具、LangGraph、测试和优化相关问答。 |
| `docs/interview-prep/运行流程.md` | 运行流程说明材料。 |

### `多阶段详细优化/`

| 文件 | 作用 |
|---|---|
| `多阶段详细优化/第一阶段优化技术方案.md` | 第一阶段技术方案，重点是稳定工具层和统一返回协议。 |
| `多阶段详细优化/第一阶段验收文档.md` | 第一阶段验收记录，说明公共工具模块、返回协议和工具改造结果。 |
| `多阶段详细优化/第二阶段优化技术方案.md` | 第二阶段技术方案，重点是补基础测试和明确测试依赖。 |
| `多阶段详细优化/第二阶段验收文档.md` | 第二阶段验收记录，说明测试套件扩展和依赖确认结果。 |
| `多阶段详细优化/第三阶段优化技术方案.md` | 第三阶段技术方案，重点是结构化状态和阶段执行器。 |
| `多阶段详细优化/第三阶段验收文档.md` | 第三阶段验收记录，说明状态扩展、阶段执行器和流程改造结果。 |
| `多阶段详细优化/第四阶段优化技术方案.md` | 第四阶段技术方案，重点是拆分 Prompt 并收口 Prompt 注册。 |
| `多阶段详细优化/第四阶段验收文档.md` | 第四阶段验收记录，说明 Prompt 拆分和统一导出结果。 |
| `多阶段详细优化/第五阶段优化技术方案.md` | 第五阶段技术方案，重点是配置中心和环境变量管理。 |
| `多阶段详细优化/第五阶段验收文档.md` | 第五阶段验收记录，说明配置中心、示例配置和安全校验结果。 |
| `多阶段详细优化/第六阶段优化技术方案.md` | 第六阶段技术方案，重点是 Redis 缓存和 checkpoint 持久化。 |
| `多阶段详细优化/第六阶段验收文档.md` | 第六阶段验收记录，说明 Redis 配置、依赖和降级策略结果。 |
| `多阶段详细优化/第七阶段优化技术方案.md` | 第七阶段技术方案，重点是后端流式事件协议和前端体验优化。 |
| `多阶段详细优化/第七阶段验收文档.md` | 第七阶段验收记录，说明流式事件增强、前端优化和测试覆盖结果。 |
| `多阶段详细优化/第七阶段补充优化方案-前置计划摘要与降级提示.md` | 第七阶段补充方案，说明前置计划摘要和降级提示的优化设计。 |
| `多阶段详细优化/第七阶段补充优化方案-Tavily搜索替换.md` | 第七阶段补充方案，说明从 DuckDuckGo 补充或切换到 Tavily 搜索的设计。 |
| `多阶段详细优化/fallback降级说明.md` | fallback 降级说明，解释降级含义、原因和项目中的具体示例。 |

## 本地生成目录和缓存目录

| 目录 | 作用 |
|---|---|
| `.git/` | Git 版本库元数据目录。 |
| `.venv/` | Python 虚拟环境目录，保存本地安装的依赖。 |
| `.idea/` | JetBrains IDE 项目配置目录。 |
| `.jbeval/` | JetBrains 相关本地评估或辅助目录。 |
| `.pytest_cache/` | pytest 本地缓存目录。 |
| `__pycache__/` | Python 字节码缓存目录。 |

这些本地生成目录通常不需要手动维护，也不应该作为项目业务代码阅读重点。
