# 旅游规划机器人修改内容说明

> 原项目目录：`E:\BaiduNetdiskDownload\AI\langGraph_day01-day05\旅游规划机器人（原）`  
> 原项目 GitHub 链接：`https://github.com/JianXiao2021/travel_agent_LLM.git`  
> 修改后目录：`E:\py_project\Travel_llm`  
> 更新时间：2026-06-29

## 一、总体变化

原项目是一个基于 LangGraph 的旅游规划机器人示例，核心结构是：

```text
Agent -> ToolNode -> Agent
```

模型根据提示词自由决定是否调用工具，工具执行完再回到 Agent，直到模型生成最终回复。界面主要通过 Gradio 展示聊天内容和工具调试信息，状态结构只保存 `messages`。

修改后项目将原来的单 Agent 循环式工具调用，改造成了更工程化的多阶段旅游规划 Agent：

```text
intent_router
  -> clarification_responder 或 attraction_collector
  -> itinerary_planner
  -> transport_validator
  -> poi_enricher
  -> final_responder
```

整体方向从“模型自由调用工具”调整为“先识别需求，再按规划阶段执行”。每个阶段负责明确的子任务，阶段之间通过结构化状态传递目的地、用户约束、景点、每日行程、交通校验、餐饮住宿和异常提示。

## 二、核心架构修改

### 1. LangGraph 工作流重构

原项目文件：

- `graph/graph.py`
- `agents/agents.py`
- `states/state.py`
- `prompts/main.py`

原项目只定义了两个节点：

- `agent`：负责思考、生成回复、决定是否调用工具；
- `tools`：通过 LangGraph `ToolNode` 执行工具。

修改后新增和调整：

- 新增 `graph/nodes.py`，将业务拆为多个节点：
  - `IntentRouterNode`：识别用户意图、目的地和约束；
  - `ClarificationResponderNode`：目的地缺失时直接反问；
  - `AttractionCollectorNode`：采集景点并补全坐标；
  - `ItineraryPlannerNode`：生成每日行程；
  - `TransportValidatorNode`：校验景点间交通可行性；
  - `PoiEnricherNode`：补充餐饮和住宿推荐；
  - `FinalResponderNode`：整理最终 Markdown 回复。
- 新增 `graph/routers.py`，集中处理条件路由：
  - 缺少目的地时进入澄清节点；
  - 用户确认已有方案时直接进入最终回复；
  - 交通校验失败时有限次数重排行程；
  - 正常流程进入周边推荐和最终回复。
- 修改 `graph/graph.py`，从 `Agent -> ToolNode -> Agent` 改为多节点 `StateGraph`。

### 2. 状态结构扩展

原项目 `states/state.py` 只有：

```python
messages: Annotated[list, add_messages]
```

修改后扩展为 `TravelPlannerState`，新增字段包括：

- `intent_type`：用户意图类型；
- `selected_destination`：识别出的目的地；
- `user_constraints`：天数、预算、偏好等约束；
- `candidate_destinations`：候选目的地；
- `attractions`：景点数据；
- `daily_plan`：每日行程；
- `transport_segments`：交通路线结果；
- `meal_options` / `hotel_options`：餐饮和住宿推荐；
- `validation_issues`：流程中的校验问题；
- `needs_clarification`：是否需要澄清；
- `needs_replan` / `replan_attempts`：是否需要重规划及重试次数。

这样做的好处是，后续节点不再只依赖长对话上下文，而是可以直接读取结构化字段，流程更稳定，也更便于测试。

## 三、LLM 调用方式修改

### 1. 新增阶段执行器

新增文件：

- `agents/stage_executor.py`

原项目中，`AsyncAgent` 将完整系统提示词和历史消息交给模型，由模型自己决定下一步。

修改后新增 `StageExecutor`，统一处理每个阶段的 LLM 调用：

- Prompt 格式化；
- 状态摘要注入；
- 异步模型调用；
- JSON 输出解析；
- JSON 解析失败 fallback；
- 模型调用异常 fallback。

这使得意图识别、行程生成、交通校验等阶段可以各自拥有独立提示词和容错逻辑，避免一个阶段输出异常导致整个流程中断。

### 2. Prompt 拆分

原项目将所有规则写在 `prompts/main.py` 的一个大提示词中。

修改后拆分为多个阶段提示词：

- `prompts/intent.py`
- `prompts/clarify.py`
- `prompts/planner.py`
- `prompts/validation.py`
- `prompts/finalize.py`
- `prompts/main.py`

这样每个节点只关注当前阶段任务，Prompt 更短，也更容易维护和测试。

### 3. 模型适配调整

原项目 `models/factory.py` 主要支持 OpenAI `gpt-*` 模型。

修改后新增：

- `models/qwen_factory.py`

当前项目通过 DashScope OpenAI 兼容接口调用模型，默认模型为 `deepseek-v4-flash`，配置由 `.env` 和 `settings.py` 统一管理。

## 四、接口和前端修改

### 1. 新增 FastAPI 服务

新增文件：

- `api/main.py`
- `run_fastapi.py`
- `chat_service.py`

新增接口：

- `GET /`：返回前端页面；
- `GET /health`：健康检查；
- `POST /sessions`：创建会话并返回 `thread_id`；
- `POST /chat/stream`：聊天流式接口。

`/chat/stream` 使用 `application/x-ndjson` 返回按行分隔的 JSON 事件，前端可以边生成边展示结果。

### 2. 流式事件标准化

原项目 Gradio 直接读取 LangGraph 的底层事件，只区分：

- 模型输出；
- 工具开始；
- 工具结束。

修改后在 `chat_service.py` 中将底层事件统一整理为：

- `progress`：阶段进度；
- `chunk`：模型文本片段；
- `tool_start`：工具开始；
- `tool_end`：工具结束；
- `tool_error`：工具失败；
- `degradation`：降级提示；
- `error`：后端异常；
- `done`：本轮完成。

同时过滤内部阶段的模型流，只把最终回复和必要的前置计划摘要展示给用户，避免用户看到内部 JSON 或调试内容。

### 3. 新增 Vue 单页前端

新增文件：

- `templates/index.html`

当前主要入口从 Gradio 演示页扩展为 FastAPI + Vue 单页前端，支持：

- 新建会话；
- 停止生成；
- 本地历史会话；
- Markdown 导出；
- 工具调用调试面板；
- 降级提示展示；
- 错误详情分层展示。

原 `webrun.py` 保留，用于兼容原 Gradio 演示方式。

## 五、工具层修改

### 1. 统一工具响应结构

新增文件：

- `tools/base.py`

工具返回结构统一为：

```json
{
  "success": true,
  "data": {},
  "error": null
}
```

失败时统一为：

```json
{
  "success": false,
  "data": null,
  "error": {
    "message": "error_code",
    "detail": "详细信息",
    "retryable": true
  }
}
```

这样 Agent 节点可以稳定判断工具是否成功，并把失败信息写入 `validation_issues` 或转换为前端事件。

### 2. 外部接口重试和日志

`tools/base.py` 新增：

- HTTP 超时控制；
- 429 / 5xx 等可重试状态判断；
- 退避重试；
- 统一日志；
- 安全参数摘要，避免日志泄露敏感信息。

地图、路线、周边等工具统一复用 `request_json_with_retry`。

### 3. 景点抓取降级

原项目使用 Selenium 抓取马蜂窝景点数据。

修改后在 `tools/attractions.py` 中增强：

- 主数据源仍优先使用马蜂窝；
- Selenium 启动失败、页面结构变化、景点列表为空时，自动降级到 Web Search；
- 降级结果带有 `fallback_used` 和 `warnings`；
- 前端可展示“主数据源失败，已使用搜索结果”的用户可读提示；
- 只缓存马蜂窝主数据源成功结果，不缓存 Web Search fallback 结果，避免一次降级污染长期结果。

### 4. Web Search Provider 扩展

原项目使用 DuckDuckGo 搜索。

修改后 `tools/web_search.py` 支持：

- `duckduckgo`
- `tavily`

通过环境变量 `WEB_SEARCH_PROVIDER` 切换。Tavily 的返回字段被映射为项目统一字段：

- `title`
- `href`
- `body`
- `score`

这样可以在 DuckDuckGo 限流时切换到 Tavily，提高演示稳定性。

### 5. 高德地图工具增强

修改后的地图相关工具包括：

- `tools/locations.py`
- `tools/nearby.py`
- `tools/transportation.py`

增强内容：

- 缺少 `AMAP_API_KEY` 时返回明确错误；
- 请求结果加入 Redis 缓存；
- 接口异常时返回结构化错误；
- 路线规划保留步行距离、出租车费用、公共交通方案列表；
- 无公共交通方案时通过 `warnings` 提醒。

## 六、缓存和会话持久化修改

### 1. 新增 Redis 工具缓存

新增文件：

- `utils/redis_cache.py`

缓存内容包括：

- 景点抓取结果；
- Web 搜索结果；
- 地点坐标；
- 周边 POI；
- 交通路线。

缓存 key 使用 namespace + 请求参数哈希，避免不同目的地、不同搜索 provider 或不同路线互相污染。

Redis 不可用时，缓存自动退化为 miss，不影响主流程运行。

### 2. 新增 LangGraph checkpoint 持久化

新增文件：

- `graph/checkpoint.py`

原项目使用 `InMemorySaver`，进程重启后会话状态丢失。

修改后新增 `RedisBackedMemorySaver`：

- 兼容 LangGraph `InMemorySaver`；
- 将 checkpoint 快照写入 Redis；
- 支持 TTL；
- Redis 不可用时自动回退到内存。

这样可以支持跨会话状态恢复，也保留了本地演示环境的容错能力。

## 七、配置管理修改

新增文件：

- `settings.py`
- `.env.example`

原项目主要依赖 `load_dotenv` 和代码中的固定参数。

修改后集中管理配置，包括：

- `APP_ENV`
- `MODEL_NAME`
- `DASHSCOPE_BASE_URL`
- `DASHSCOPE_API_KEY`
- `AMAP_API_KEY`
- `WEB_SEARCH_PROVIDER`
- `TAVILY_API_KEY`
- `REQUEST_TIMEOUT_SECONDS`
- `MAX_RETRIES`
- `CACHE_ENABLED`
- `REDIS_URL`
- `REDIS_KEY_PREFIX`
- `REDIS_CHECKPOINT_ENABLED`
- `CORS_ORIGINS`
- `REQUIRE_API_KEYS`

`settings.py` 会对配置进行类型转换和合法性校验，例如生产环境禁止使用 `*` 作为 CORS 来源、超时时间必须大于 0、搜索 provider 必须在允许列表中。

## 八、测试体系新增

原项目没有独立测试目录。

修改后新增 `tests/`，覆盖：

- `test_api_smoke.py`：FastAPI 基础接口；
- `test_settings.py`：配置读取和校验；
- `test_graph_workflow.py`：LangGraph 路由和节点行为；
- `test_graph_checkpoint.py`：checkpoint 行为；
- `test_redis_cache.py`：Redis 缓存服务；
- `test_streaming_and_prompts.py`：流式事件、Prompt 渲染、fallback；
- `test_tools_attractions.py`：景点工具和降级逻辑；
- `test_tools_web_search.py`：DuckDuckGo / Tavily 搜索；
- `test_tools_locations.py`：坐标工具；
- `test_tools_nearby.py`：周边 POI；
- `test_tools_transportation.py`：路线规划；
- `test_tools_base.py`：工具层公共重试和错误处理。

测试重点不是验证第三方真实接口，而是验证：

- 工具成功路径；
- 工具失败路径；
- 降级路径；
- 配置异常；
- 状态字段更新；
- 前端事件格式；
- 敏感字段脱敏。

## 九、文档和项目结构新增

新增或重写：

- `README.md`
- `基于LangGraph的旅游规划机器人.md`
- `项目总体优化建议.md`
- `项目优化计划.md`
- `多阶段详细优化/`
- `docs/interview-prep/`

这些文档用于说明：

- 项目启动方式；
- 环境变量配置；
- 多阶段工作流；
- 工具能力；
- 降级提示；
- Redis 缓存与 checkpoint；
- 测试方式；
- 面试讲解材料。

## 十、依赖文件调整

原项目依赖文件：

- `req.txt`

修改后调整为：

- `requirements.txt`

并补充当前项目需要的依赖，例如：

- FastAPI / Uvicorn；
- LangGraph / LangChain；
- DashScope 兼容模型相关依赖；
- Redis；
- Pytest；
- Selenium；
- BeautifulSoup；
- DuckDuckGo Search；
- Tavily 通过 HTTP API 调用。

## 十一、保留内容

以下能力仍然保留，并在修改后项目中继续使用或兼容：

- LangGraph 作为核心编排框架；
- Gradio 演示入口 `webrun.py`；
- 马蜂窝景点抓取；
- 高德地图坐标、周边和路线工具；
- Web Search 工具；
- 静态地图工具；
- 旅游规划中文 Prompt 思路。

但它们不再是一个完全由模型自由循环控制的工具链，而是被纳入更明确的多阶段工作流中。

## 十二、修改前后对比总结

| 对比项 | 原项目 | 修改后项目 |
|---|---|---|
| Agent 形态 | ReAct 风格，Agent 与 ToolNode 循环 | 多阶段 Plan-and-Execute 工作流 |
| 状态结构 | 仅 `messages` | 结构化旅游规划状态 |
| 流程控制 | 模型自由决定下一步 | LangGraph 条件路由控制 |
| Prompt | 一个大 Prompt | 按阶段拆分 Prompt |
| 前端 | Gradio | FastAPI + Vue 单页前端，Gradio 保留 |
| 接口 | 无独立后端 API | 新增 `/sessions`、`/chat/stream` 等接口 |
| 流式输出 | 直接展示模型流和工具调试 | 标准化 NDJSON 事件 |
| 工具异常 | 主要依赖模型自行处理 | 统一 success / data / error、重试、降级、用户可读错误 |
| 搜索 | DuckDuckGo | DuckDuckGo / Tavily 可切换 |
| 缓存 | 无统一工具缓存 | Redis TTL 缓存 |
| 会话持久化 | InMemorySaver | Redis checkpoint，失败回退内存 |
| 配置 | 分散在代码和 `.env` | `settings.py` 集中校验 |
| 测试 | 无独立测试体系 | 新增 Pytest 测试覆盖核心路径 |
| 文档 | 较少 | README、优化方案、验收文档、面试材料 |

## 十三、当前项目运行方式

安装依赖：

```powershell
cd "E:\py_project\Travel_llm"
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

复制配置：

```powershell
Copy-Item .env.example .env
```

启动 FastAPI + Vue 前端：

```powershell
.\.venv\Scripts\python.exe run_fastapi.py
```

访问：

```text
http://127.0.0.1:8000/
```

启动原 Gradio 演示入口：

```powershell
.\.venv\Scripts\python.exe webrun.py
```

运行测试：

```powershell
.\.venv\Scripts\python.exe -m pytest
```

## 十四、一句话概括

本次修改将原来的“基于 LangGraph 的工具调用演示项目”，升级为“具备多阶段流程编排、结构化状态、FastAPI 流式接口、Vue 前端、工具降级、Redis 缓存与 checkpoint、自动化测试”的旅游规划 Agent 工程化项目。

## 十五、面试表达参考

如果面试官问这个项目，可以这样描述：

我这个项目是在一个原始的 LangGraph 旅游规划机器人基础上做的二次开发和工程化改造。原项目更接近 ReAct 风格，主要是一个 Agent 和 ToolNode 循环，模型自己判断什么时候调用景点搜索、地图、路线规划等工具。我改造后把它调整成了多阶段 Plan-and-Execute 工作流，把旅游规划拆成了意图识别、目的地澄清、景点采集、行程生成、交通校验、餐饮住宿补充和最终回复几个明确节点，每个节点只负责一个阶段的任务，节点之间通过结构化状态传递目的地、用户约束、景点、路线和校验问题。

这样改的原因是，旅游规划不是简单问答，它对流程稳定性要求比较高。如果完全交给模型自由调用工具，容易出现重复调用、漏掉校验、缺少目的地还继续查工具等问题。所以我在 LangGraph 里做了条件路由：比如用户没说目的地时直接进入澄清节点，不再调用搜索和地图；用户确认已有方案时可以直接进入最终回复；交通校验发现路线不合理时再触发有限次数的重规划。

在工程实现上，我封装了一个 LLM 阶段执行器，统一处理 Prompt 格式化、异步模型调用、JSON 输出解析和解析失败 fallback。外部工具方面，我把马蜂窝景点抓取、高德地图坐标和路线、Tavily / DuckDuckGo 搜索都封装成 LangChain Tool，并统一了 `success / data / error` 返回结构。针对 Selenium 抓取失败、搜索限流、地图接口超时这些不稳定场景，我做了重试、降级提示和用户可读错误，前端可以区分“结果可用但数据不完整”和“工具调用失败”。

最后我还补了 FastAPI 流式接口和 Vue 单页前端，把 LangGraph 底层事件标准化成 `progress`、`chunk`、`tool_start`、`tool_end`、`degradation`、`error`、`done` 这些 NDJSON 事件，方便前端实时展示生成进度和工具状态。同时引入 Redis 做工具结果 TTL 缓存和 LangGraph checkpoint，减少重复外部查询，也支持会话状态恢复。测试方面，我用 Pytest 覆盖了图路由、配置校验、流式事件、工具成功和失败路径、Redis 缓存和 checkpoint。

如果面试官追问“这个项目最核心的改动是什么”，可以回答：

核心改动是把原来的单 Agent 自由工具调用，改成了可控的多阶段 Agent 工作流。这个改动让流程从“模型自己想下一步”变成“系统按旅游规划业务流程推进”，再配合结构化状态、条件路由、工具降级和缓存，使项目更稳定，也更像一个可以交付的后端 AI 应用。

如果面试官追问“为什么不用纯 ReAct”，可以回答：

ReAct 适合开放式问题探索，但旅游规划有比较明确的业务阶段，比如先确定目的地，再拿景点，再排行程，再校验交通。如果完全依赖 ReAct，模型可能重复查工具、遗漏交通校验，或者在信息不足时继续执行重工具。这个项目更适合 Plan-and-Execute，所以我用 LangGraph 把关键阶段固定下来，再在每个阶段内部使用 LLM 和工具完成具体任务。
