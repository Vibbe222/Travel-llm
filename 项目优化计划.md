# 项目优化计划

这份计划用于把当前旅游规划机器人从 Demo 逐步优化成更稳定、可维护、可测试的工程化项目。

## 第 1 阶段：稳定工具层

目标：让所有外部工具调用可控、可恢复、可测试。

### 任务

1. 统一工具返回格式。

   成功时：

   ```python
   {
       "success": True,
       "data": {},
       "error": None,
   }
   ```

   失败时：

   ```python
   {
       "success": False,
       "data": None,
       "error": {
           "message": "错误类型",
           "detail": "错误详情",
       },
   }
   ```

2. 给外部请求加超时和异常处理。

   重点文件：

   - `tools/nearby.py`
   - `tools/transportation.py`
   - `tools/locations.py`
   - `tools/web_search.py`
   - `tools/attractions.py`

   优化方向：

   - 增加 `timeout`；
   - 增加 `raise_for_status()`；
   - 明确捕获网络异常、解析异常、接口错误；
   - 不直接抛出不可控异常，而是返回统一错误结构。

3. 清理工具里的 `print`。

   将调试输出改为 `logging`，建议记录：

   - 工具名；
   - 输入摘要；
   - 调用耗时；
   - 成功或失败状态；
   - 错误类型。

4. 降低 Selenium 失败影响。

   `tools/attractions.py` 失败时不应中断整体规划，而是返回可解释的失败信息，让 Agent 可以降级使用网页搜索结果补充。

### 产出

- 工具层返回格式统一；
- 外部 API 失败可解释；
- 工具调用过程可观测；
- 后续测试更容易编写。

## 第 2 阶段：补基础测试

目标：先把最容易坏的工具层保护起来。

### 任务

1. 新增 `tests/` 目录。
2. 使用 mock 模拟高德、DuckDuckGo、马蜂窝等外部响应。
3. 覆盖工具层典型场景：
   - 成功返回数据；
   - API 返回错误；
   - 网络超时；
   - 空结果；
   - 缺少环境变量。
4. 增加 API 冒烟测试：
   - `/health`
   - `/sessions`
   - `/chat/stream` 基础请求结构。

### 产出

- 初步测试套件；
- 修改工具或接口时能快速发现回归；
- 外部接口异常场景有明确保障。

## 第 3 阶段：重构 LangGraph 状态和流程

目标：降低大 Prompt 控制复杂流程的不确定性。

### 任务

1. 将旧的 `PublicState` 升级为结构化状态。
2. 按已迁移的结构化状态设计，加入以下字段：
   - `selected_destination`
   - `user_constraints`
   - `attractions`
   - `daily_plan`
   - `transport_segments`
   - `meal_options`
   - `hotel_options`
   - `validation_issues`
3. 把当前单一 Agent 拆成多个节点：
   - 意图识别节点；
   - 目的地澄清节点；
   - 景点采集节点；
   - 行程规划节点；
   - 交通校验节点；
   - 餐住推荐节点；
   - 最终回复节点。
4. 将参考实现迁移到正式入口，并在验证通过后删除 `new/` 历史目录。

### 产出

- 图结构更清晰；
- 每个节点职责更单一；
- 工具结果能沉淀到结构化状态；
- 后续扩展新能力更容易。

## 第 4 阶段：拆分 Prompt

目标：让 Prompt 更短、更稳定、更容易调试。

### 任务

1. 将 `prompts/main.py` 拆成多个文件：
   - `intent.py`
   - `clarify.py`
   - `planner.py`
   - `validation.py`
   - `finalize.py`
2. 每个 Prompt 只负责一个阶段。
3. 对需要结构化输出的节点，要求模型输出 JSON。
4. 对 JSON 解析失败增加 fallback。

### 产出

- Prompt 更容易维护；
- 模型输出更可控；
- 单个阶段出错时更容易定位。

## 第 5 阶段：配置与安全

目标：让项目适合本地、测试、生产多环境运行。

### 任务

1. 新增 `settings.py`，统一管理：
   - 模型名；
   - DashScope base_url；
   - API key；
   - 请求超时；
   - CORS；
   - 缓存开关；
   - 日志级别。
2. 新增 `.env.example`。
3. 启动时集中校验环境变量。
4. 收紧 `api/main.py` 的 CORS 配置，生产环境不再使用 `allow_origins=["*"]`。

### 产出

- 配置来源统一；
- 本地和生产环境更容易区分；
- 密钥和跨域配置更安全。

## 第 6 阶段：缓存与会话持久化

目标：减少重复外部请求，避免服务重启丢状态。

### 任务

1. 给以下查询增加 TTL 缓存：
   - 地点坐标；
   - 景点信息；
   - 周边 POI；
   - 交通路线；
   - 搜索结果。
2. 将 `InMemorySaver` 替换为持久化 checkpointer。
3. 可选方案：
   - 本地演示：SQLite；
   - 服务部署：Postgres 或 Redis。

### 产出

- 重复查询更快；
- 外部接口压力更低；
- 服务重启后会话状态可恢复。

## 第 7 阶段：前端体验优化

目标：让用户更容易理解生成过程和失败原因。

### 任务

1. 增加“停止生成”按钮。
2. 增加“新建会话”按钮。
3. 工具调用面板支持折叠。
4. 错误信息分层展示：
   - 用户可读错误；
   - 调试详情；
   - 工具原始返回。
5. 增加历史会话或当前会话导出功能。

### 产出

- 用户能主动中断长时间生成；
- 会话管理更清晰；
- 工具失败时更容易理解原因；
- 前端更接近可用产品。

## 推荐实施顺序

1. 工具返回格式统一。
2. 外部请求超时和错误处理。
3. 工具层测试。
4. 结构化状态迁移。
5. 多节点 LangGraph 重构。
6. Prompt 拆分。
7. 配置中心化。
8. 缓存和会话持久化。
9. 前端体验优化。
10. 部署与文档完善。

## 最优先做的任务

建议先做“工具返回格式统一 + 外部请求错误处理”。

这是整个项目最关键的底座。工具层稳定后，Agent 才能可靠地规划、失败后降级，前端才能展示清晰错误，后续测试和 LangGraph 重构也更容易推进。

