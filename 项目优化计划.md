# 项目优化计划

更新时间：2026-06-30

这份计划用于跟踪旅游规划机器人从 Demo 到工程化项目的优化进度。根据当前代码、验收文档和测试结果复核，原计划第 1 到第 7 阶段的主体工作已经落地，但仍有少量待收尾项需要继续处理。

## 当前总体状态

| 阶段 | 主题 | 当前状态 | 结论 |
|---|---|---|---|
| 第 1 阶段 | 稳定工具层 | 已完成 | 工具统一返回 `success/data/error`，外部请求有超时、HTTP 状态检查、异常捕获、日志和 Selenium 降级。 |
| 第 2 阶段 | 补基础测试 | 部分完成 | 已有工具层、API、图流程、配置、Redis 和流式事件测试；但 2026-06-30 全量 pytest 仍有 3 个配置相关失败。 |
| 第 3 阶段 | 重构 LangGraph 状态和流程 | 已完成 | 已升级 `TravelPlannerState`，并拆成多节点 LangGraph，历史 `new/` 目录已删除。 |
| 第 4 阶段 | 拆分 Prompt | 已完成 | 已拆出 `intent.py`、`clarify.py`、`planner.py`、`validation.py`、`finalize.py`，并提供 JSON fallback。 |
| 第 5 阶段 | 配置与安全 | 已完成 | 已新增 `settings.py`、`.env.example`，模型、API key、超时、CORS、缓存和日志集中配置。 |
| 第 6 阶段 | 缓存与会话持久化 | 已完成 | 已接入 Redis TTL 缓存和 Redis-backed checkpointer，Redis 不可用时降级为无缓存或内存 checkpoint。 |
| 第 7 阶段 | 前端体验优化 | 已完成 | 已有停止生成、新建会话、历史会话、导出、工具折叠、进度事件、错误提示和降级提示框。 |

## 验证记录

2026-06-30 执行：

```powershell
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
79 collected
76 passed
3 failed
```

失败项：

| 测试 | 当前问题 |
|---|---|
| `tests/test_settings.py::test_settings_defaults_are_development_safe` | 测试期望默认搜索 provider 是 `duckduckgo`，当前环境/配置实际为 `tavily`。 |
| `tests/test_settings.py::test_settings_requires_api_keys_in_production` | 测试期望生产环境缺少 key 会抛错，但当前配置没有触发该错误。 |
| `tests/test_tools_web_search.py::test_web_search_tavily_requires_api_key` | 测试期望缺少 Tavily key 返回 `missing_tavily_api_key`，当前实际进入 Tavily 请求并返回超时。 |

这说明基础测试体系已经建立，但配置测试隔离和 Tavily key 校验仍需修复，暂不能标记为“全量测试通过”。

## 第 1 阶段：稳定工具层

目标：让所有外部工具调用可控、可恢复、可测试。

### 达成情况

- 已新增 `tools/base.py`，提供 `ok()`、`fail()`、`request_json_with_retry()`、统一日志和参数摘要。
- `tools/locations.py`、`tools/nearby.py`、`tools/transportation.py`、`tools/web_search.py`、`tools/attractions.py` 已统一返回：

```python
{
    "success": True,
    "data": {},
    "error": None,
}
```

失败时：

```python
{
    "success": False,
    "data": None,
    "error": {
        "message": "错误类型",
        "detail": "错误详情",
        "retryable": False,
    },
}
```

- 外部请求已加入 `timeout`、`raise_for_status()`、重试和异常捕获。
- Selenium 景点抓取失败时会降级到网页搜索 fallback，不再直接中断整体规划。
- 工具日志已改为 `logging`，会记录工具名、耗时、状态和安全化后的输入摘要。

### 剩余问题

- `tools/*.py` 中仍有少量 `if __name__ == "__main__"` 示例调试 `print()`，不影响线上工具调用，但如追求彻底整洁可以清理。
- Selenium 仍是脆弱依赖，长期建议替换为更稳定的数据源或官方 API。

## 第 2 阶段：补基础测试

目标：先把最容易坏的工具层保护起来。

### 达成情况

- 已新增 `tests/` 目录。
- 已覆盖工具层成功、失败、空结果、网络异常、缺少环境变量等场景。
- 已覆盖 API 冒烟测试：
  - `/health`
  - `/sessions`
  - `/chat/stream`
- 已补充：
  - `tests/test_graph_workflow.py`
  - `tests/test_graph_checkpoint.py`
  - `tests/test_redis_cache.py`
  - `tests/test_settings.py`
  - `tests/test_streaming_and_prompts.py`

### 剩余问题

- 全量测试当前 `76 passed / 3 failed`，需要修复配置默认值、生产环境 key 校验和 Tavily key 缺失行为的测试或实现不一致。
- 后续可补真实对话级验收用例，验证多轮修改、目的地澄清、工具降级后的最终回复质量。

## 第 3 阶段：重构 LangGraph 状态和流程

目标：降低大 Prompt 控制复杂流程的不确定性。

### 达成情况

- `states/state.py` 已升级为 `TravelPlannerState`，包含：
  - `selected_destination`
  - `user_constraints`
  - `candidate_destinations`
  - `attractions`
  - `daily_plan`
  - `transport_segments`
  - `meal_options`
  - `hotel_options`
  - `validation_issues`
  - `final_status`
  - `needs_clarification`
  - `needs_replan`
- `graph/nodes.py` 已拆成多个节点：
  - `intent_router`
  - `clarification_responder`
  - `destination_clarifier`
  - `attraction_collector`
  - `itinerary_planner`
  - `transport_validator`
  - `poi_enricher`
  - `final_responder`
- `graph/graph.py` 已使用 `StateGraph(TravelPlannerState)` 编排流程。
- 历史 `new/` 参考目录已不存在。

### 剩余问题

- 节点逻辑已经拆开，但还有进一步增强空间，例如更严格的状态 schema 校验、节点级质量评估和更完整的多轮修改策略。

## 第 4 阶段：拆分 Prompt

目标：让 Prompt 更短、更稳定、更容易调试。

### 达成情况

- `prompts/main.py` 已不再承担完整流程控制。
- 已新增阶段 Prompt：
  - `prompts/intent.py`
  - `prompts/clarify.py`
  - `prompts/planner.py`
  - `prompts/validation.py`
  - `prompts/finalize.py`
- `agents/stage_executor.py` 已提供 `expects_json` 和 JSON 解析失败 fallback。
- 意图识别、行程规划等阶段已要求结构化 JSON 输出。

### 剩余问题

- Prompt 仍可继续补节点级评测样例，尤其是 JSON 输出稳定性和中文边界表达。

## 第 5 阶段：配置与安全

目标：让项目适合本地、测试、生产多环境运行。

### 达成情况

- 已新增 `settings.py`，统一管理：
  - 模型名
  - DashScope base_url
  - API key
  - 请求超时
  - 最大重试次数
  - CORS
  - 缓存开关
  - Redis 配置
  - 日志级别
  - 搜索 provider
- 已新增 `.env.example`。
- `api/main.py` 已从 settings 读取 CORS，生产环境禁止通配来源。
- 模型工厂、工具超时、缓存和 checkpoint 已接入配置中心。

### 剩余问题

- 当前全量测试暴露出配置默认值和生产 key 校验预期不一致，需要统一实现与测试口径。
- `api/main.py` 请求体目前只用普通 `BaseModel` 字段，后续可增加长度、空值、坐标范围等 Pydantic 约束。

## 第 6 阶段：缓存与会话持久化

目标：减少重复外部请求，避免服务重启丢状态。

### 达成情况

- 已新增 `utils/redis_cache.py`。
- 以下查询已接入 Redis TTL 缓存：
  - 地点坐标
  - 景点信息
  - 周边 POI
  - 交通路线
  - 搜索结果
- 已新增 `graph/checkpoint.py`：
  - `RedisBackedMemorySaver`
  - `build_checkpointer()`
- Redis 可用时使用 Redis-backed checkpoint。
- Redis 包缺失、连接失败或写入失败时，会降级为无缓存或 `InMemorySaver`，主流程可继续运行。

### 剩余问题

- 当前 Redis-backed checkpointer 是项目内轻量适配器，生产规模化建议评估官方 Redis/Postgres checkpointer。
- 前端已经能展示降级提示，但缓存命中状态还可以进一步独立展示。

## 第 7 阶段：前端体验优化

目标：让用户更容易理解生成过程和失败原因。

### 达成情况

- `templates/index.html` 已实现：
  - 停止生成
  - 新建会话
  - 历史会话
  - 当前会话导出
  - 历史会话导出
  - 工具调用面板折叠
  - 流式进度展示
  - 降级提示框
  - 错误信息分层展示
- `chat_service.py` 已将 LangGraph 事件规范化为 NDJSON：
  - `progress`
  - `chunk`
  - `tool_start`
  - `tool_end`
  - `tool_error`
  - `degradation`
  - `error`
  - `done`

### 剩余问题

- 前端仍是单文件 Vue + CDN 方式，适合本地演示；若要产品化，建议迁移到独立前端工程并补浏览器端自动化测试。
- 当前历史会话保存在浏览器 localStorage，服务端历史会话管理还未实现。

## 后续优先事项

1. 修复当前 3 个失败测试，让全量 pytest 重新变绿。
2. 统一 Tavily / DuckDuckGo 的默认 provider 策略、API key 校验策略和测试隔离方式。
3. 给 `ChatRequest` 增加 Pydantic 字段约束，限制空消息、超长输入和非法 `thread_id`。
4. 同步更新 `docs/interview-prep/` 中仍描述旧 `InMemorySaver`、旧路径或旧流程的内容。
5. 清理 `__pycache__`、`.pytest_cache`、`.idea` 等本地产物，避免进入版本控制。
6. 增加 Dockerfile、部署说明和健康检查细节。

## 推荐实施顺序

原始优化路线已经基本完成。下一步建议按以下顺序收尾：

1. 修复失败测试。
2. 补输入校验。
3. 更新旧面试文档和 README 中可能残留的过期描述。
4. 清理本地产物和无关缓存。
5. 补生产部署文档。
6. 评估是否引入官方 Redis/Postgres checkpointer。
